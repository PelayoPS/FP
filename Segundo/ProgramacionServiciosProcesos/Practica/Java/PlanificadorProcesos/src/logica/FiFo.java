package logica;

public class FiFo implements Algoritmo {

    /**
     * Ejecuta un algoritmo First in First out
     * Devuelve una matriz de string con los resultados
     * @return String[][]
     */
    @Override
    public String[] execute(Proceso[] procesos) {
        String[] resultados = new String[procesos.length];
        // mete una representación con proceso emoji emoji
        // de la duración de cada proceso y su tiempo de espera
        int tiempoActual = 0;
        char usedTime = Algoritmo.usedTime;
        char wastedTime = Algoritmo.wastedTime;
        

        return resultados;
        
    }
}
